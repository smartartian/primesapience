# 原初智性设计系统

版本：1.0

设计系统预览：`design-system.html`

## 核心原则

1. 清晰先于聪明：用户先理解，再感受到智能。
2. 信号多于噪声：用层级、空间和节奏建立重点。
3. 技术保持温度：精密但不冰冷，接近真实工作。
4. 最终能够交付：所有规则都可以被稳定实现和复用。

## 视觉方向

- 深色是语境，品牌蓝是信号。
- 中文使用系统无衬线字体，微标签和技术信息使用等宽字体。
- 以 4px 为最小空间单位，主要布局使用 8、16、24、48、72、96、140。
- 交互控件圆角使用 6px，重复卡片使用 8px，标签使用胶囊形。
- 动效以反馈和空间关系为主，不使用无意义漂浮效果。
- 不嵌套卡片，不使用超大圆角、装饰性光斑或与业务无关的通用科技图片。

## 核心令牌

共享令牌位于 `tokens.css`。

```css
--yc-night-900: #090c0f;
--yc-brand-500: #05a8e5;
--yc-brand-300: #83dff8;
--yc-paper-100: #f1f0e9;
--yc-signal-400: #c7e964;
--yc-ember-400: #ff8152;
```

品牌色占比建议：

- 70% 深色基底
- 20% 纸张与中性色
- 10% 品牌与状态色

## 组件语言

- 主要按钮：品牌蓝底、深色文字、右上箭头。
- 次要按钮：透明底、细分隔线、下行箭头。
- 标签：仅用于分类和状态，不代替按钮。
- 能力卡：编号、标题、结果导向说明，圆角不超过 8px。
- 场景卡：深色和纸张色交替，标签放在底部。
- 导航：细边框胶囊容器，当前状态只使用短下划线。

## 内容语气

说结果，不堆术语。优先描述用户能获得什么，例如“把分散知识变成随时可调用的助手”，而不是“全面赋能企业智能升级”。

推荐结构：

1. 业务背景
2. 当前问题
3. 解决路径
4. 明确产出
5. 验证方式

## 使用规则

- 新页面必须先引入 `tokens.css`。
- 品牌色只用于关键动作、活动状态和少量数据强调。
- 任何文本都必须通过桌面和移动端溢出检查。
- 交互控件必须有 hover、focus、active 和 disabled 状态。
- 所有动画必须支持 `prefers-reduced-motion`。
- 新增组件前先确认现有组件无法通过组合完成任务。
